/**
 * Renders a simple Features component that displays the text "Features".
 *
 * @returns {JSX.Element} A div element containing the string "Features".
 */
export default function Features() {
  return <div>Features</div>;
}